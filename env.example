# GPU server
VLLM_HOST=
SPEACHES_HOST=

HF_TOKEN=
# Shared API key for backend services (vLLM, infinity-emb, docling)
BACKEND_API_KEY=sk-backend-change-me

# LiteLLM
LITELLM_MASTER_KEY=sk-local-dev        # change in production!
LITELLM_SALT_KEY=salt-change-me-32chr  # random string, never change after first run

# Admin UI
UI_USERNAME=admin
UI_PASSWORD=changeme

# PostgreSQL
POSTGRES_USER=litellm
POSTGRES_PASSWORD=changeme
POSTGRES_DB=litellm
